/**
 * file: basicmath.js
 */

export default class BasicMath {

  constructor(){ }

  // unreachable
  multiply(frst, second){ 
	return frst * second;
  }

  exponent(base, exp){
	return Math.pow(base, exp);
  }

}
